private static void queuestackdeque() {
    // Queue
    // 선입선출(FIFO)
    Queue<Integer> queue = new LinkedList<>();

    // Stack
    // 후입선출(LIFO)
    Stack<Integer> stack = new Stack<>();

    // Deque
    // 양방향 큐
    Deque<Integer> deque = new ArrayDeque<>();

    // Queue 메서드
    // add(): 요소 추가
    // poll(): 요소 제거 및 값 반환

    // Stack 메서드
    // push(): 요소 추가
    // pop(): 요소 제거 및 값 반환

    // Deque 메서드
    // 앞쪽에 저장된 요소: first
    // 뒤쪽에 저장된 요소: last

    // 공통 메서드
    // peek(): 다음 제거될 요소 값 반환
    // contains(): 저장된 요소 중 값 확인
    // isEmpty(): 자료구조가 비어있는지 확인
    // size(): 저장된 요소 개수 반환
}
